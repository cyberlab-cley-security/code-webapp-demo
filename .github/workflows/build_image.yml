name: Docker Build and Push to ECR

on:
  workflow_dispatch:
    inputs:
      ecr_registry:
        description: 'AWS ECR registry URL (e.g., 123456789.dkr.ecr.us-east-1.amazonaws.com)'
        required: true
        type: string
      image_name:
        description: 'Docker image name'
        required: true
        type: string
        default: 'cloudlabs-webapp'
      image_tag:
        description: 'Docker image tag'
        required: true
        type: string
        default: 'latest'
      aws_region:
        description: 'AWS region'
        required: true
        type: string
        default: 'us-east-1'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      # Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
      
      # Configure AWS credentials using GitHub secrets
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ github.event.inputs.aws_region }}

      # Login to Amazon ECR
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      
      # Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      # Build Docker image locally
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: ${{ github.event.inputs.image_name }}:scan
          cache-from: type=registry,ref=${{ github.event.inputs.ecr_registry }}/${{ github.event.inputs.image_name }}:buildcache
      
      # Install Docker Scout CLI
      - name: Install Docker Scout
        run: |
          docker scout version || docker scout update
      
      # Run Docker Scout scan
      - name: Scan image with Docker Scout
        run: |
          echo "Scanning image ${{ github.event.inputs.image_name }}:scan with Docker Scout..."
          docker scout cves ${{ github.event.inputs.image_name }}:scan --json > scout-results.json
          cat scout-results.json
      
      # Optionally upload scan results to GitHub (SARIF not yet supported nativement)
      - name: Upload Docker Scout results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: docker-scout-results
          path: scout-results.json
      
      # Build and push Docker image to ECR
      - name: Build and push Docker image to ECR
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ github.event.inputs.ecr_registry }}/${{ github.event.inputs.image_name }}:${{ github.event.inputs.image_tag }}
            ${{ github.event.inputs.ecr_registry }}/${{ github.event.inputs.image_name }}:${{ github.run_number }}
          cache-from: type=registry,ref=${{ github.event.inputs.ecr_registry }}/${{ github.event.inputs.image_name }}:buildcache
          cache-to: type=registry,ref=${{ github.event.inputs.ecr_registry }}/${{ github.event.inputs.image_name }}:buildcache,mode=max
      
      # Display success message
      - name: Image pushed successfully
        run: |
          echo "✅ Image successfully built and pushed to ECR"
          echo "Registry: ${{ github.event.inputs.ecr_registry }}"
          echo "Image: ${{ github.event.inputs.image_name }}"
          echo "Tags: ${{ github.event.inputs.image_tag }}, ${{ github.run_number }}"
